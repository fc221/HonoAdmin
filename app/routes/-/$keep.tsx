/**
 * Placeholder island so honox's `<Script>` emits the client bundle.
 * `$`-prefix files are recognized as islands but not registered as routes.
 */
export default function Keep() {
  return null
}
